[package]
name = "memory-everyday-desktop-widget"
version = "0.1.0"
description = "MemoryEveryDay desktop calendar widget"
authors = ["MemoryEveryDay"]
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }

[features]
custom-protocol = ["tauri/custom-protocol"]
